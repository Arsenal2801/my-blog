---
const { items } = Astro.props;
---

<ul class="bulleted-list">
  {items.map(item => <li>{item}</li>)}
</ul>

<style>
  .bulleted-list {
    padding-left: 1.5rem;
    margin: 1rem 0;
  }
  .bulleted-list li {
    margin-bottom: 0.5rem;
  }
</style>
